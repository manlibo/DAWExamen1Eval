--EJERCICIO 1--
a.
Comandos utilizados:
git init
git add .
git commit

b.
git remote add origin https://github.com/manlibo/DAWExamen1Eval.git
git branch -M main
git push -u origin main

c. 
Captura de pantalla en el documento de examen

d.
Captura de pantalla en el documento de examen

e.
Creamos archivo .gitignore y escribimos dentro el archivo y la carpeta que queremos ignorar: private.txt y privada

f. 
Captura de pantalla en el documento del examen

g. 
para añadir una etiqueta utilizamos el comando:
git tag "v1"

h.
Subimos los cambios al repositorio:
git add .
git commit
git push
git push --tags

--EJERCICIO 2--

a.
Para crear una rama:
git branch "v0.2"

b.
Creamos fichero 2.txt en rama v0.2
Cambiamos a esta rama con:
git checkout "v0.2"

d.
Para realizar un merge, hacemos commit desde rama v0.2, nos situamos en la rama main y hacemos git merge "v0.2"

--EERCICIO 3--
a.
Captura en el documento.
Para hacer el commit:
git add .
git commit -m "Hacemos commit de ejercicio 3 apartado a." 

b.
git checkout "v0.2"
y hacemos los cambios (adios) y el commit desde esta rama.

c.
nos posicionamos en main con git checkout "main"
Hacemos git merge "v0.2"
Aceptamos los dos cambios para solucionar el conflicto y nos quedamos con el texto Hola y adiós.

--EJERCICIO 4--
a.
Listamos las ramas con merge con el comando: git branch --merged

b.
Creamos el tag v0.2 con tag v0.2
lo subimos con git push --tags

c.
Borramos la rama v0.2 con el comando:
git branch -d "v0.2"
